module.exports = {
  module: {
    rules: [
      {
        test: /\.(js|jsx)$/,
        exclude: /node_modules/,
        use: {
          loader: 'babel-loader',
          options: {
            presets: ['@babel/preset-env', '@babel/preset-react', '@babel/preset-flow'],
          },
        },
      },
      {
        test: /\.(png|jpe?g|gif)$/i,
        use: [
          {
            loader: 'file-loader',
          },
        ],
      },
      {
        test: /ReactNativeTypes\.js$/,
        loader: 'ignore-loader',
      },
      {
        test: /requireNativeComponent\.js$/,
        loader: 'ignore-loader',
      },
    ],
  },
};